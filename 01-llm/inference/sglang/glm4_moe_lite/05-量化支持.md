# 量化支持

## 1. QuantizationConfig 基类

### 1.1 源码位置

`layers/quantization/base_config.py`

### 1.2 导入

```python
# glm4_moe_lite.py:51
from sglang.srt.layers.quantization.base_config import QuantizationConfig
```

### 1.3 核心接口

```python
# layers/quantization/base_config.py
class QuantizationConfig:
    def get_name(self) -> str:
        """返回量化方法名称"""
        ...

    def get_quant_method(self, layer, prefix: str):
        """返回指定层的量化方法"""
        ...

    def get_moe_method(self):
        """返回 MoE 专用的量化方法"""
        ...
```

## 2. get_moe_impl_class - MoE 实现选择

### 2.1 源码位置

`layers/moe/ep_moe/layer.py`

### 2.2 导入

```python
# glm4_moe_lite.py:48
from sglang.srt.layers.moe.ep_moe.layer import get_moe_impl_class
```

### 2.3 在 Glm4MoeLiteSparseMoeBlock 中的使用

```python
# glm4_moe_lite.py:237-249
self.experts = get_moe_impl_class(quant_config)(
    num_experts=config.n_routed_experts
    + self.num_fused_shared_experts
    + get_global_server_args().ep_num_redundant_experts,
    num_fused_shared_experts=self.num_fused_shared_experts,
    top_k=config.num_experts_per_tok + self.num_fused_shared_experts,
    hidden_size=config.hidden_size,
    intermediate_size=config.moe_intermediate_size,
    layer_id=self.layer_id,
    quant_config=quant_config,
    routed_scaling_factor=self.routed_scaling_factor,
    prefix=add_prefix("experts", prefix),
)
```

### 2.4 实现选择逻辑

```python
# layers/moe/ep_moe/layer.py
def get_moe_impl_class(quant_config: Optional[QuantizationConfig]):
    if quant_config is None:
        return FusedMoE  # 无量化
    return quant_config.get_moe_method()  # 返回对应量化方法
```

## 3. 量化类型检测

### 3.1 INT8 检测

```python
# glm4_moe_lite.py:268-274, 292-295
self.shared_experts_is_int8 = False

if config.n_shared_experts is not None and self.num_fused_shared_experts == 0:
    ...
    is_packed_weight = hasattr(
        self.shared_experts.gate_up_proj.quant_method, "quant_config"
    )
    self.shared_experts_is_int8 = (
        not is_packed_weight
        and self.shared_experts.gate_up_proj.weight.dtype == torch.int8
    )
```

### 3.2 FP8 检测

```python
# glm4_moe_lite.py:296-299
self.shared_experts_is_fp8 = (
    not is_packed_weight
    and self.shared_experts.gate_up_proj.weight.dtype == torch.float8_e4m3fn
)
```

### 3.3 量化权重类型

```python
# glm4_moe_lite.py:144
self.gate_up_proj.weight.dtype == torch.uint8  # 量化权重标志
```

## 4. FP8 量化

### 4.1 FP8 数据类型

```python
torch.float8_e4m3fn  # 4 bits 指数, 3 bits 尾数
torch.float8_e5m2    # 5 bits 指数, 2 bits 尾数
```

### 4.2 FP8 常量定义

```python
# utils/common.py:109-124
if is_hip():
    HIP_FP8_E4M3_FNUZ_MAX = 224.0
    FP8_E4M3_MAX = HIP_FP8_E4M3_FNUZ_MAX
else:
    FP8_E4M3_MAX = torch.finfo(torch.float8_e4m3fn).max

FP8_E4M3_MIN = -FP8_E4M3_MAX

builtins.FP8_E4M3_MAX = FP8_E4M3_MAX
builtins.FP8_E4M3_MIN = FP8_E4M3_MIN
```

## 5. TopK 输出格式配置

### 5.1 TopKOutputFormat 枚举

```python
# layers/moe/topk.py:145-148
class TopKOutputFormat(IntEnum):
    STANDARD = auto()      # 标准格式
    TRITON_KERNEL = auto() # Triton 内核格式
    BYPASSED = auto()      # 跳过格式 (用于 FlashInfer)
```

### 5.2 在 TopK 初始化中使用

```python
# glm4_moe_lite.py:265
output_format=TopKOutputFormat.STANDARD if quant_config is None else None,
# 无量化时使用 STANDARD 格式
# 有量化时由后端自动选择
```

## 6. FP4 MoE 支持

### 6.1 FP4 All-Gather 检测

```python
# glm4_moe_lite.py:46-47
from sglang.srt.layers.moe import (
    should_use_flashinfer_cutlass_moe_fp4_allgather,
)

# glm4_moe_lite.py:283-286
if (
    get_moe_a2a_backend().is_deepep()
    or get_moe_a2a_backend().is_mooncake()
    or should_use_flashinfer_cutlass_moe_fp4_allgather()
):
    # 禁用共享专家 TP
    ...
```

## 7. 权重打包处理

### 7.1 检测打包权重

```python
# glm4_moe_lite.py:135-139
if not hasattr(self.gate_up_proj, "weight"):
    self.gate_up_proj.weight = getattr(self.gate_up_proj, "weight_packed")
if not hasattr(self.down_proj, "weight"):
    self.down_proj.weight = getattr(self.down_proj, "weight_packed")
```

### 7.2 is_packed_weight 标志

```python
# glm4_moe_lite.py:289-291
is_packed_weight = hasattr(
    self.shared_experts.gate_up_proj.quant_method, "quant_config"
)
```
