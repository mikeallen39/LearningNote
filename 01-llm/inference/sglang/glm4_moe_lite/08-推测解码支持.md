# 推测解码支持

## 1. NextN 层 (MTP)

### 1.1 is_nextn 参数

```python
# glm4_moe_lite.py:165, 205, 331, 474
# is_nextn 参数在多个组件中传递

class Glm4MoeLiteGate(nn.Module):
    def __init__(self, config, prefix: str = "", is_nextn: bool = False):
        self.is_nextn = is_nextn

class Glm4MoeLiteSparseMoeBlock(DeepseekV2MoE):
    def __init__(self, ..., is_nextn: bool = False):
        self.is_nextn = is_nextn

class Glm4MoeLiteDecoderLayer(DeepseekV2DecoderLayer):
    def __init__(self, ..., is_nextn: bool = False):
        ...
```

### 1.2 NextN 层 ID 计算

```python
# glm4_moe_lite.py:554-565 (在 load_weights 中)
if is_nextn:
    if hasattr(self.config, "num_nextn_predict_layers"):
        num_nextn_layers = self.config.num_nextn_predict_layers
        assert num_nextn_layers == 1, "Only 1 nextn layer is supported"
        # 兼容旧设计
        nextn_layer_id = (
            0
            if self.config.num_hidden_layers == 1
            else self.config.num_hidden_layers
        )
    else:
        raise ValueError("num_nextn_predict_layers is not in the config")
```

### 1.3 NextN 权重映射

```python
# glm4_moe_lite.py:612-666
if is_nextn:
    nextn_layer_prefix = f"model.layers.{nextn_layer_id}"
    nextn_spec_weight_names = [
        "shared_head.norm",
        "eh_proj",
        "enorm",
        "hnorm",
    ]
else:
    nextn_layer_prefix = None
    nextn_spec_weight_names = []

# 权重名称映射
if nextn_layer_prefix and not name.startswith(nextn_layer_prefix):
    continue

if nextn_layer_prefix is not None:  # mtp
    # 使用共享的 head 和 embed 权重
    if "shared_head.head" in name or "embed_tokens" in name:
        continue

    is_decoder = True
    for weight_name in nextn_spec_weight_names:
        if weight_name in name:
            name = name.replace(nextn_layer_prefix, "model")
            is_decoder = False
            break
    if is_decoder:
        name = name.replace(nextn_layer_prefix, "model.decoder")
```

### 1.4 NextN 对 Router 优化的影响

```python
# glm4_moe_lite.py:180-184
if (
    _is_cuda
    and not self.is_nextn  # NextN 层禁用此优化
    and hidden_states.shape[0] < 4
    and hidden_states.shape[1] == 7168
    and self.weight.shape[0] == 256
    and _device_sm >= 90
):
    from sgl_kernel import dsv3_router_gemm
    logits = dsv3_router_gemm(hidden_states, self.weight)
```

## 2. EAGLE 支持

### 2.1 is_eagle 参数

```python
# glm4_moe_lite.py:550, 624-629
def load_weights(
    self,
    weights: Iterable[Tuple[str, torch.Tensor]],
    is_nextn=False,
    params_dict=None,
    is_eagle=False,  # EAGLE 标志
):
    ...

eagle_ignore_weight_names = []
if is_eagle:
    eagle_ignore_weight_names = [
        "eagle_draft_tokens_map",
        "eagle_lm_head.weight",
    ]
```

### 2.2 忽略 EAGLE 特定权重

```python
# glm4_moe_lite.py:728-729
if name in eagle_ignore_weight_names:
    continue
```

## 3. 隐藏状态捕获

### 3.1 capture_aux_hidden_states

```python
# glm4_moe_lite.py:502
self.capture_aux_hidden_states = False
```

### 3.2 LogitsProcessorOutput 中的 hidden_states

```python
# layers/logits_processor.py:66-107
@dataclasses.dataclass
class LogitsProcessorOutput:
    next_token_logits: Optional[torch.Tensor]
    hidden_states: Optional[torch.Tensor] = None  # EAGLE 使用
```

## 4. 层稀疏判断

### 4.1 is_layer_sparse 方法

```python
# glm4_moe_lite.py:365
self.is_layer_sparse = self._is_layer_sparse(layer_id, is_nextn=is_nextn)
```

### 4.2 NextN 层始终为稀疏层

```python
# DeepseekV2DecoderLayer._is_layer_sparse (继承)
def _is_layer_sparse(self, layer_id: int, is_nextn: bool = False) -> bool:
    if is_nextn:
        return True  # NextN 层始终为稀疏层
    ...
```

## 5. MLP/MoE 选择

### 5.1 根据 is_layer_sparse 选择

```python
# glm4_moe_lite.py:377-399
if self.is_layer_sparse:
    self.mlp = Glm4MoeLiteSparseMoeBlock(
        config=config,
        quant_config=quant_config,
        prefix=add_prefix("mlp", prefix),
        layer_id=self.layer_id,
        alt_stream=alt_stream,
        is_nextn=is_nextn,
    )
else:
    if enable_moe_dense_fully_dp():
        mlp_tp_rank, mlp_tp_size = 0, 1
    else:
        mlp_tp_rank, mlp_tp_size = None, None
    self.mlp = Glm4MoeLiteMLP(
        hidden_size=config.hidden_size,
        intermediate_size=config.intermediate_size,
        hidden_act=config.hidden_act,
        quant_config=quant_config,
        prefix=add_prefix("mlp", prefix),
        tp_rank=mlp_tp_rank,
        tp_size=mlp_tp_size,
    )
```

## 6. 层通信配置

### 6.1 is_last_layer 判断

```python
# glm4_moe_lite.py:411-414
self.layer_communicator = LayerCommunicator(
    ...
    is_last_layer=(
        is_nextn or (self.layer_id == self.config.num_hidden_layers - 1)
    ),
    ...
)
```

## 7. moe_layer_freq 配置

### 7.1 强制所有层为 MoE 层

```python
# glm4_moe_lite.py:477
config.moe_layer_freq = 1  # 所有层都是 MoE 层
```
